# Environment Configuration for Rust REST API
# 
# Copy this file to .env and update the values for your local development environment.
# The .env file should never be committed to version control as it may contain sensitive information.

# Database Configuration
# Individual database connection parameters
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Alternative: Full Database URL (for future use with connection pooling libraries)
# Format: mssql://username:password@host:port/database_name
# DATABASE_URL=mssql://sa:your_password@192.168.102.120:1433/TEST_1

# Logging Configuration
# Set the log level for the application
# Options: trace, debug, info, warn, error
# Example: RUST_LOG=info
# RUST_LOG=info

# Server Configuration (currently hardcoded in main.rs)
# In future versions, these could be configurable:
# SERVER_HOST=127.0.0.1
# SERVER_PORT=8080

# Database Connection Pool Settings (for future implementation)
# MAX_CONNECTIONS=10
# MIN_CONNECTIONS=1
# CONNECTION_TIMEOUT=30
